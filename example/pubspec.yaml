name: bitmap_example
description: Demonstrates how to use the bitmap plugin.
publish_to: 'none'

environment:
  sdk: ">=3.0.3 <4.0.0"
  flutter: ">=3.13.9 <4.0.0"

dependencies:
  bitmap:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true
  assets:
    - assets/doggo.jpeg
    - assets/street.jpg
