name: bitmap
description: A fast and minimalistic lib to manipulate images made specifically for Flutter
version: 0.2.0
repository: https://github.com/bluefireteam/bitmap
topics: [image, image-processing, ffi]


environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  ffi: ^2.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  ffigen: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.1.0
