name: bitmap
description: A minimalist package to help you manipulate bitmaps. It is focused on bitmap transformations.
version: 0.0.3
author: Renan C. Araújo <renan@caraujo.me>
homepage: https://github.com/renancaraujo/bitmap

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.example.bitmap
    pluginClass: BitmapPlugin
